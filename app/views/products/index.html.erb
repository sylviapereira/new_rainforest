<h1>All Products</h1>
<%= link_to "New Product", new_product_path %>

<% @products.each do |p| %>
    <p><%= link_to p.name, product_path(p) %>, <%= p.description %>, <%= p.price_in_cents %></p>
    <% end %>
